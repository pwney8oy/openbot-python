Comandi per tutti:
!owner = in privato: ritorna l'owner/s e il sito di openbot || !time = in chan: la data corrente || !date = in chan: la data attuale || !stats = in chan: le statistiche del canale
!peak = in chan: il picco di utenti del chan || !say [messaggio] = in chan: stampa [messaggio] || !do [messaggio] = in chan: stampa l'azione [messaggio]
!saluta [nick] = in chan: saluta [nick] || !google [query] = in chan: ritorna il primo link della [query] || !commands = in privato: stampa questo messaggio
!slap [nick] = in chan: Schiaffeggia [nick] || !wiki [query] = in chan: restituisce l'url su wikipedia di [query] || !pep = in chan: Ritorna il link dello stile del codice python
!addquote [quote] = in chan: aggiunge ai quote [quote] || !randquote = in chan: ritorna un quote casuale || !getquote [numero] = in chan: ritorna il quote numero [numero]
!news list = in chan: lista dei siti news || !news [nome] = in chan: ritorna le news di [nome] || !news [nome] [numero] = in chan: ritorna l'url, il titolo e la descrizione della news [numero] su [nome]
!incula nick = in chan action: il bot incula [nick] || !inculap nick = in chan action: il bot viene inculato da [nick] || !seen nick = in chan: ritorna l'ultima volta che il bot ha visto [nick]
!uptime = in privato: ritorna il tempo passato dall'avvio dell'OpenBot || !weather [citta'] = in chan: ritorna il tempo e i gradi di [citta']
